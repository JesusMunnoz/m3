import { CodigoLibrosPipe } from './codigo-libros.pipe';

describe('CodigoLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigoLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
